type BelongsTo {
  _id: String
  type: String
}

type Post {
  _id: String
  title: String
  author: String
  authorId: String
  upvote: Int
  downvote: Int
  score: Int
  numComment: Int
  belongsTo: BelongsTo
}

type Query {
  posts: [Post]
  post(id: String): Post
}
