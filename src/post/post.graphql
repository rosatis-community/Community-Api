type Post {
  _id: String
  title: String
  author: User
  upvote: Int
  downvote: Int
  score: Int
  numComment: Int
  community: Community
}

type Query {
  posts: [Post]
  post(id: String): Post
}
